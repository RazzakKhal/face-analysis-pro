import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.goldenratio.analyzer', // identifiant unique du package
  appName: 'Golden Ratio Face Analyzer', // nom affiché sur le téléphone
  webDir: 'www'
};

export default config;
